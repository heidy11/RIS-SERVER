// Este archivo es un alias para /dicom.route.js y evita rutas duplicadas.
// Las rutas activas deben ser  /api/dicom/convert-image-to-dicom y /api/dicom/upload-image.

module.exports = require('./dicom.route.js');
